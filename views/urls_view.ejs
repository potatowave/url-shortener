
<!DOCTYPE html>
<html lang="en">

  <head></head>

  <body>
    <% include partials/_header %>
    <form action="/urls/<%= shortURL %>" method="POST">
      <label for="newURL">Enter a URL:</label>
      <input id="newURL" type="text" name="newURL" placeholder="http://" style="width: 300px" value="<%= urlDatabase[shortURL] %>">
      <input type="submit" value="Change">
    </form>
    <a href="../u/<%= shortURL %>"><%= shortURL %></a> is currently pointing to <a href="<%= urlDatabase[shortURL] %>"><%= urlDatabase[shortURL] %></a>
  </body>
</html>